name: ui_kit
description: App resources.
version: 0.0.8
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter

#  resources:
#    git:
#      url: git@github.com:tarek360/flutter_kit.git
#      ref: 0.0.8 # or a specific branch, tag, or commit
#      path: flutter_kit/resources

  design_system:
    path: ../design_system
#  design_system:
#    git:
#      url: git@github.com:tarek360/flutter_kit.git
#      ref: 0.0.8 # or a specific branch, tag, or commit
#      path: flutter_kit/design_system

  ui_kit_interface:
    path: ../ui_kit_interface
#  ui_kit_interface:
#    git:
#      url: git@github.com:tarek360/flutter_kit.git
#      ref: 0.0.8 # or a specific branch, tag, or commit
#      path: flutter_kit/ui_kit_interface


  dots_indicator: ^3.0.0
  flutter_animate: ^4.5.2
  top_snackbar_flutter: ^3.0.0+1
  figma_squircle: # It was on 0.5.3 before being broken by Flutter 3.27.0. This is a fork that fixes it.
    git:
      url: https://github.com/Ax0elz/figma_squircle
      ref: 5f1ad5aaccdf31fc398fc141979ea845a0f45383 # The ref that fixes the issue

dev_dependencies:
  flutter_test:
    sdk: flutter
  global_lints:
    git:
      url: git@github.com:tarek360/flutter_kit.git
      ref: 0.0.8
      path: flutter_kit/global_lints
flutter:

  fonts:
    - family: icons
      fonts:
        - asset: assets/icon_font/icons.ttf